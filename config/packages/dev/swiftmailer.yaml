# See https://symfony.com/doc/current/email/dev_environment.html
swiftmailer:
    url: '%env(MAILER_URL)%'
    spool: { type: 'memory' }
    transport: smtp
    auth_mode: login
    port: 465
    host: smtp.gmail.com
    username: adjagarail@gmail.com
    password: koenigsegg
    # send all emails to a specific address
    #delivery_addresses: ['me@example.com']
